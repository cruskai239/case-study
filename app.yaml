runtime: php
env: flex

runtime_config:
  document_root: public

# Ensure we skip ".env", which is only for local development
skip_files:
  - .env

env_variables:
  # Put production environment variables here.
  APP_LOG: errorlog
  APP_DEBUG: true
  APP_ENV: local
  APP_KEY: base64:IxkDTeqmFS/vS0zjJKPVr+lfs5lkzM9O7sL9rSwYc/U=
  STORAGE_DIR: /tmp
  DB_CONNECTION: mysql
  DB_HOST: 35.196.103.227
  DB_PORT: 3306
  DB_DATABASE: case_study
  DB_USERNAME: ruskai
  DB_PASSWORD: ruskai123$